{
  "name": "mcp-toolkit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev --config packages/backend/tauri.conf.json",
    "build": "tauri build --config packages/backend/tauri.conf.json",
    "test": "pnpm -r --parallel test"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
